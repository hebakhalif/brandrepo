import 'package:flutter/material.dart';

class ManCategory extends StatelessWidget {
  const ManCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('woman category'),
    );
  }
}
